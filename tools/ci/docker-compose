#!/usr/bin/env bash
# Run Docker Compose as the invoking user so bind-mounted artifacts stay writable.
set -euo pipefail

export DOCKER_UID="${DOCKER_UID:-$(id -u)}"
export DOCKER_GID="${DOCKER_GID:-$(id -g)}"

exec docker compose "$@"
